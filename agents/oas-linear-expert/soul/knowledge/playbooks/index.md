# Playbooks

* [Probe an OAS package against a released kernel before shipping](released-kernel-consumer-probe.md) - Drive the package through a real released OAS CLI in an isolated sandbox to prove consumer install, trust, command, template, and spawn behavior.
